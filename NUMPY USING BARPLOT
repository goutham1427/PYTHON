import numpy as np
import matplotlib.pyplot as plt

overs= np.arange(1,9)
score=[5,6,7,20,12,16,8,10]
plt.bar(overs,score, color='green')
plt.title("INDIA SCORE OVER BY OVER")
plt.xlabel("over number")
plt.ylabel("runs scored")
plt.xticks(overs)
plt.grid(axis='y',alpha=0.7)
plt.show()
