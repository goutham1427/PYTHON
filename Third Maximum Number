def thirdMax(nums):
    nums=list(set(nums))
    if (len(nums)<=3):
        return max(nums)
    nums.sort(reverse=True)
    return nums[2]
    
nums=list(map(int,input().split()))

print(thirdMax(nums))
